% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/routing.R
\name{ox_k_shortest_paths}
\alias{ox_k_shortest_paths}
\title{k shortest paths between two nodes}
\usage{
ox_k_shortest_paths(g, from, to, k = 3, weight = "length")
}
\arguments{
\item{g}{An \link[=new_osm_graph]{osm_graph}.}

\item{from, to}{Node \code{osmid}s.}

\item{k}{Number of paths to return. Default \code{3}.}

\item{weight}{Edge column used as weight. Default \code{"length"}.}
}
\value{
A \link[tibble:tibble]{tibble} with one row per path: \code{rank}, \code{cost} and
a list-column \code{path} of node \code{osmid}s, ordered by increasing cost. Fewer
than \code{k} rows are returned when fewer distinct paths exist.
}
\description{
Computes up to \code{k} loopless shortest paths from \code{from} to \code{to} using Yen's
algorithm in the Rust core. Useful for route alternatives.
}
\examples{
g <- example_osm_graph()
from <- ox_nearest_nodes(g, 0, 0)
to <- ox_nearest_nodes(g, 200, 200)
ox_k_shortest_paths(g, from, to, k = 3)
}
