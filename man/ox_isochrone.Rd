% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/isochrone.R
\name{ox_isochrone}
\alias{ox_isochrone}
\title{Compute isochrones (service areas)}
\usage{
ox_isochrone(g, center, cutoffs, weight = "length", ratio = 0.4)
}
\arguments{
\item{g}{An \link[=new_osm_graph]{osm_graph}.}

\item{center}{One or more origin node \code{osmid}s (see \code{\link[=ox_nearest_nodes]{ox_nearest_nodes()}}).}

\item{cutoffs}{Numeric vector of cutoff values, in the units of \code{weight}.}

\item{weight}{Edge column used as cost. Default \code{"length"}.}

\item{ratio}{Concavity for \code{sf::st_concave_hull()} (0 = most concave,
1 = convex). Default \code{0.4}.}
}
\value{
An \code{sf} object with one polygon row per cutoff (columns \code{cutoff},
\code{n_nodes}, \code{geometry}), ordered from largest to smallest cutoff so smaller
areas draw on top.
}
\description{
For one or more origin nodes, finds the set of nodes reachable within each
\code{cutoff} (by the chosen edge \code{weight} — distance or, with
\code{\link[=ox_add_edge_travel_times]{ox_add_edge_travel_times()}}, travel time) and returns a polygon per cutoff:
the hull of the reachable nodes. With several origins, reachability is the
minimum cost from any origin.
}
\details{
Reachable sets come from the Rust Dijkstra core; the hull is built with
\code{sf::st_concave_hull()} when available (GEOS >= 3.11), falling back to a
convex hull. For metric cutoffs, project the graph to a metric CRS first.
}
\examples{
g <- example_osm_graph(n = 6, spacing = 100)
center <- ox_nearest_nodes(g, 250, 250)
iso <- ox_isochrone(g, center, cutoffs = c(100, 300))
iso
}
