% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/speeds.R
\name{ox_add_edge_speeds}
\alias{ox_add_edge_speeds}
\title{Add edge speeds}
\usage{
ox_add_edge_speeds(g, speeds = NULL, fallback = 40)
}
\arguments{
\item{g}{An \link[=new_osm_graph]{osm_graph}.}

\item{speeds}{Optional named numeric vector of \code{highway = kph} overrides,
merged over the built-in defaults.}

\item{fallback}{Speed (km/h) for edges with no matching class. Default \code{40}.}
}
\value{
The \link[=new_osm_graph]{osm_graph} with a \code{speed_kph} edge column.
}
\description{
Assigns a free-flow speed (km/h) to every edge based on its \code{highway} class,
adding a \code{speed_kph} column. Unknown classes get \code{fallback}.
}
\examples{
g <- example_osm_graph()
g <- ox_add_edge_speeds(g, speeds = c(residential = 25))
head(g$edges$speed_kph)
}
