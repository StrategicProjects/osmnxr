% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simplify.R
\name{ox_consolidate_intersections}
\alias{ox_consolidate_intersections}
\title{Consolidate nearby intersections}
\usage{
ox_consolidate_intersections(g, tolerance = 10)
}
\arguments{
\item{g}{An \link[=new_osm_graph]{osm_graph}.}

\item{tolerance}{Distance below which nodes are merged, in CRS units.
Default \code{10}.}
}
\value{
A consolidated \link[=new_osm_graph]{osm_graph} (with \code{meta$consolidated = TRUE}).
}
\description{
Merges groups of nodes lying within \code{tolerance} of one another into single
nodes placed at the group centroid, then rewrites edges to the consolidated
nodes and drops the resulting self-loops. Useful for collapsing the multiple
OSM nodes that represent one complex junction (e.g. dual carriageways).
}
\details{
Clustering uses \code{sf::st_is_within_distance()}; connected components are found
by the Rust core. \code{tolerance} is in the units of the graph CRS, so project
the graph first (e.g. to a metric CRS) for a meaningful distance.
}
\examples{
g <- example_osm_graph(n = 4, spacing = 100)
# nothing is within 10 units here, so the graph is unchanged
ox_consolidate_intersections(g, tolerance = 10)
}
