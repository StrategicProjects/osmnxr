% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/speeds.R
\name{ox_add_edge_travel_times}
\alias{ox_add_edge_travel_times}
\title{Add edge travel times}
\usage{
ox_add_edge_travel_times(g)
}
\arguments{
\item{g}{An \link[=new_osm_graph]{osm_graph}.}
}
\value{
The \link[=new_osm_graph]{osm_graph} with \code{speed_kph} and \code{travel_time}
edge columns.
}
\description{
Adds a \code{travel_time} edge column (in seconds) from edge \code{length} (metres) and
\code{speed_kph}. Speeds are added with \code{\link[=ox_add_edge_speeds]{ox_add_edge_speeds()}} first if missing.
The resulting column can be used as a routing \code{weight} for time-based
shortest paths and isochrones.
}
\examples{
g <- example_osm_graph()
g <- ox_add_edge_travel_times(g)
from <- ox_nearest_nodes(g, 0, 0)
to <- ox_nearest_nodes(g, 300, 300)
ox_shortest_path(g, from, to, weight = "travel_time")
}
