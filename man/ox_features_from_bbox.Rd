% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/features.R
\name{ox_features_from_bbox}
\alias{ox_features_from_bbox}
\title{Download features within a bounding box}
\usage{
ox_features_from_bbox(bbox, tags)
}
\arguments{
\item{bbox}{Numeric \code{c(xmin, ymin, xmax, ymax)} in longitude/latitude.}

\item{tags}{Named list of OSM tag filters. Each element is either \code{TRUE}
(key present with any value) or a character vector of allowed values, e.g.
\code{list(amenity = c("school", "hospital"))}.}
}
\value{
An \code{sf} of \code{POINT} features with \code{osm_type}, \code{osm_id} and one column
per tag encountered.
}
\description{
Queries OpenStreetMap (via Overpass) for elements matching \code{tags} — points of
interest, amenities, buildings, transit stops, and so on — returning them as
an \code{sf} of points (ways and relations are represented by their centroid).
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
bbox <- c(-34.91, -8.07, -34.87, -8.04)
ox_features_from_bbox(bbox, tags = list(amenity = "school"))
\dontshow{\}) # examplesIf}
}
