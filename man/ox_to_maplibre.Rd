% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{ox_to_maplibre}
\alias{ox_to_maplibre}
\title{Build a MapLibre GL style fragment}
\usage{
ox_to_maplibre(
  g,
  path,
  source_id = "osmnxr",
  layer_id = "streets",
  url = basename(path)
)
}
\arguments{
\item{g}{An \link[=new_osm_graph]{osm_graph}.}

\item{path}{GeoJSON output path for the edge data.}

\item{source_id, layer_id}{Identifiers for the MapLibre source and layer.}

\item{url}{URL the style should use to fetch the data. Defaults to
\code{basename(path)}.}
}
\value{
A named list with \code{sources} and \code{layers}, invisibly written data to
\code{path}.
}
\description{
Writes the edges to a GeoJSON file and returns a MapLibre GL JS style
fragment (a list with \code{sources} and \code{layers}) that references it, ready to
merge into a map style. Serialize with, e.g., \code{jsonlite::toJSON(..., auto_unbox = TRUE)}.
}
\examples{
g <- example_osm_graph()
style <- ox_to_maplibre(g, tempfile(fileext = ".geojson"))
names(style)
}
