% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{ox_centrality}
\alias{ox_centrality}
\title{Node centrality}
\usage{
ox_centrality(
  g,
  type = c("betweenness", "closeness"),
  weight = "length",
  normalized = TRUE
)
}
\arguments{
\item{g}{An \link[=new_osm_graph]{osm_graph}.}

\item{type}{Centrality measures to compute: any of \code{"betweenness"} and
\code{"closeness"}. Default both.}

\item{weight}{Edge column used as weight. Default \code{"length"}.}

\item{normalized}{Scale scores for comparability across graphs. Betweenness
is divided by \code{(n - 1)(n - 2)}; closeness uses the Wasserman--Faust
correction for disconnected graphs. Default \code{TRUE}.}
}
\value{
A \link[tibble:tibble]{tibble} with column \code{osmid} plus one column per
requested measure.
}
\description{
Computes betweenness and/or closeness centrality for every node, using the
Rust core (Brandes' algorithm for betweenness; one Dijkstra per node for
closeness).
}
\examples{
g <- example_osm_graph(n = 4)
ox_centrality(g, type = "betweenness")
}
