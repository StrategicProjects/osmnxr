% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/settings.R
\name{ox_settings}
\alias{ox_settings}
\title{Get or set package settings}
\usage{
ox_settings(...)
}
\arguments{
\item{...}{Named settings to update. Recognised names: \code{overpass_url},
\code{nominatim_url}, \code{user_agent}, \code{timeout}, \code{max_tries}, \code{cache}.}
}
\value{
A named list of settings (current values, or the previous values
invisibly when updating).
}
\description{
Configure the Overpass and Nominatim endpoints, HTTP behaviour and caching
used by all \verb{ox_*} download functions. Called with no arguments it returns
the current settings as a list; called with named arguments it updates them
and returns the previous values invisibly.
}
\examples{
ox_settings()
old <- ox_settings(timeout = 300)
ox_settings(timeout = old$timeout) # restore
}
