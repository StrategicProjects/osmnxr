# Clear the session cache

Empties the in-memory cache of downloaded OpenStreetMap responses.

## Usage

``` r
ox_clear_cache()
```

## Value

Invisibly `NULL`.

## Examples

``` r
ox_clear_cache()
```
