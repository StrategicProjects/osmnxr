# Single-source shortest distances (length `n_nodes`); `Inf` if unreachable.

Single-source shortest distances (length `n_nodes`); `Inf` if
unreachable.

## Usage

``` r
rs_dijkstra(from, to, weight, n_nodes, source)
```
